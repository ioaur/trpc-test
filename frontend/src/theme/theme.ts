import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#FF7D00",
        },
        secondary: {
            main: "#303030",
        },
    },
});

export default theme;
